# Backend URL for Next.js API route handlers (server-side proxy only).
# Browser calls same-origin /api/* — no CORS setup needed on the client.
API_BASE_URL=http://localhost:8080

# Optional legacy override (prefer API_BASE_URL for local dev).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
